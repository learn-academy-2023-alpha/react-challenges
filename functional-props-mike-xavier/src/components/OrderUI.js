import React from 'react'

const OrderUI = (props) => {
  return (
   <>
        {/* <div>{props.orders.map((log) => {
                return (
                    <p>{log}</p>
                )
          })}
        </div> */}
   </>
  )
}

export default OrderUI